const styles = theme => ({
    root: {
        display: 'inline-flex',
        backgroundSize: 'cover',
        opacity: .54,
        transition: theme.transitions.create()
    }
});

export default styles;