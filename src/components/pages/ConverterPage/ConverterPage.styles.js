const styles = theme => ({
    root: {},
    header: {
        textAlign: 'center'
    },
    Button: {
        marginTop: theme.spacing.unit * 3
    },
    Grid_item: {
        marginBottom: 60
    },
    Divider: {
        maxWidth: theme.breakpoints.values.lg,
        margin: 'auto'
    }
});

export default styles;