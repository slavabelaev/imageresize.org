const styles = theme => ({
    root: {
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        maxWidth: 1170,
        minHeight: 360,
        margin: 'auto',
        textAlign: 'center'
    }
});

export default styles;