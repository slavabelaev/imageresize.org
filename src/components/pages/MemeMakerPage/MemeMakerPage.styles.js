const styles = theme => ({
    root: {},
    Grid_item: {
        marginBottom: theme.spacing.unit * 8
    },
    Divider: {
        maxWidth: theme.breakpoints.values.lg,
        margin: 'auto'
    }
});

export default styles;