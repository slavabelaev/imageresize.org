const styles = theme => ({
    root: {},
    header: {
        textAlign: 'center'
    },
    Grid_item: {
        marginBottom: theme.spacing.unit * 8
    },
    Button_getStarted: {
        marginTop: theme.spacing.unit * 3
    }
});

export default styles;