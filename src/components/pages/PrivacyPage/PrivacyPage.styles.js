const styles = theme => ({
    root: {
        maxWidth: 1170,
        margin: 'auto',
        paddingTop: theme.spacing.unit * 10,
        paddingBottom: theme.spacing.unit * 10
    }
});

export default styles;