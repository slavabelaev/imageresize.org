const styles = theme => ({
    root: {},
    colorPickerContainer: {
        display: 'flex',
        alignItems: 'center',
        '&[hidden]': {
            display: 'none'
        }
    }
});

export default styles;